{
  "name": "configuration-manager",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "reset": "node scripts/reset.mjs",
    "reset:users": "node scripts/reset.mjs users"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
